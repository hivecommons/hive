{
  "backend": "omp",
  "tailLines": 15,
  "expect": {
    "getCLIState": "ready",
    "classifyTmuxPane": "PANE_STATE_IDLE_COMPLETE",
    "paneShowsInputPrompt": true,
    "paneShowsActiveWork": false
  },
  "note": "Real tmux capture-pane -p dump (omp 18.1.16, tmux -x 200 -y 50) of the exact pane a freshly launched Hive OMP contributor sat at, captured live from a running kubestellar/hive#6626 contributor container. The splash's '# for prompt actions' hint IS present but sits above the tip-of-day banner, the update-available banner, and the status footer, so it falls outside paneTail's last-15-non-blank-line window — the original fixture (a hand-written 6-line pane) never exercised a real capture at this height and could not catch it. Readiness now comes from the persistent '%─' context-meter / '╰─' input-box chrome instead."
}
