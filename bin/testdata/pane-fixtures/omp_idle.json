{
  "backend": "omp",
  "tailLines": 15,
  "expect": {
    "getCLIState": "ready",
    "classifyTmuxPane": "PANE_STATE_IDLE_COMPLETE",
    "paneShowsInputPrompt": true,
    "paneShowsActiveWork": false
  },
  "note": "Real tmux capture-pane -p dump (omp 18.1.16, tmux -x 200 -y 50) six exchanges into a live OMP session, well after the welcome splash has scrolled off screen entirely -- zero occurrences of 'Tips', '# for prompt actions', or '/ for commands' anywhere in the pane. The original readiness patterns (anchored solely to that one-time splash text) can never match this pane, which is the one-shot-splash defect kubestellar/hive#5156/#5650 fixed for claude, recurring here because the omp fixtures never captured a second turn. The persistent '%\u2500' context-meter / '\u256f\u2500' input-box chrome still matches."
}
