- Added **Settings → Repos → Kick prompt list caps** so the kick issue/PR list caps introduced in #7368 can be set from the dashboard instead of only `hive.yaml`, which put them out of reach on hosted spokes. The panel explains plainly that the caps keep the kick prompt a manageable size for the agents that read those lists (scanner above all). Both caps are now also pinned to a ceiling of 500, so the setting can no longer be used to recreate the unbounded prompt it exists to prevent (#7368).
