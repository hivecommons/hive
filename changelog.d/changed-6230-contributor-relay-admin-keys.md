- The contributor-relay guide's admin section now documents every `contribute_*` hub config key instead of seven of them: the `allow`/`deny` mode keys for the title and author filters, `contribute_skip_assigned_to_others`, the cooldown toggle and hours, the Operations-tab queue priority and hold sets with their three authenticated endpoints (`PUT /api/contribute/queue/order`, `POST /api/contribute/queue/hold`, `POST /api/contribute/queue/hold/clear`), and `contribute_require_explicit_accept`, so an operator editing `hive.yaml` or a `PUT /api/config/governor/hub` payload can map each dashboard control to its key.
