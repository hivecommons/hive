- `pkg/hub`: completed the `saas.go` split to the v5 file boundaries — the ~12.7k-line embedded dashboard document moved out of Go and into `assets/dashboard.html` served via `go:embed` (so editors, JS linters and CI can see the frontend), and the pages, clusters, and crypto domains moved to `saas_pages.go`, `saas_clusters.go`, `saas_crypto.go`. `saas.go` drops from 13,762 to 623 lines (16,879 at the start of the split). No behavior change. Part of [#7278](https://github.com/hivecommons/hive/issues/7278).
