- Dashboard: the Advisory Digest, Hive Advice, Fleet Report Preview, Ready to level up? and Lifecycle Timeline panels are now grouped inside a single collapsible **Hive Advisory** section, instead of being scattered between the repo, beads, ACMM and platform panels. The sidebar's Advisory entry and the ACMM visibility gate now apply to the whole group.
- Dashboard: Fleet Report Preview no longer fabricates evidence for recovery entries. A recovery carries no evidence by construction, so every field fell back at once and rendered as `medium hive / unknown; count 0 in n/a`, which read like a measurement. Recoveries now state what actually happened, and the opaque fingerprint/instance identifiers moved to a hover tooltip.
