- Added unit tests for the hub admin notifications API (`src/pkg/hub/notifications_admin.go`), previously at 0% coverage: GET/PUT handler round-trips through a real temp `hive.yaml` (webhook masking, event/repo sanitization, repoScope=all clearing, masked-webhook preservation), and a full table over the `validateHubNotificationURL` SSRF guard. The guard tests document a real bypass — `https://[::1]/...` slips past the private-address check because the host is truncated at the first `:` inside the IPv6 bracket — tracked in a follow-up `[quality]` issue for a production fix.
