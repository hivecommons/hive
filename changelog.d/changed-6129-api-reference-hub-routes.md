- `src/docs/api-reference.md` now lists every hub route registered in `src/pkg/hub` ([#6129](https://github.com/hivecommons/hive/issues/6129)). [#6182](https://github.com/hivecommons/hive/pull/6182) covered the 19 routes the issue named; a fresh code-vs-doc diff of the 114 registrations on `v4` found two more with no row - the anonymously fetchable `GET /api/hub/delegation-keys` verification endpoint and the `GET /` embedded static-asset fallback - which are documented here, bringing the page to 114 of 114.
