- A new task-list sweep closes hive-filed issues automatically once every `- [ ]` box in the body is ticked AND at least one merged PR references the issue — the automation half promised by the finding-granularity guidance landed in [#7069](https://github.com/hivecommons/hive/pull/7069). Partial completion posts an idempotent progress comment (edited in place on every cycle via an HTML-comment marker, never duplicated) listing the outstanding items and the merged PRs so far; the closure comment names the merged PRs that satisfied the gate. Fail-closed hive-authorship (attribution trailer or Bot author), ≥1 checkbox required, exempt/hold labels honoured via the canonical `HasExemptLabel` mechanism, checkboxes inside fenced code blocks ignored, and a per-cycle cap bounds the blast radius. Implements [#7071](https://github.com/hivecommons/hive/issues/7071); closes the loop on [#7066](https://github.com/hivecommons/hive/issues/7066).
