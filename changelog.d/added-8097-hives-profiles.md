- Added `hivectl hives` — named hive profiles for contributors ([#8097](https://github.com/hivecommons/hive/issues/8097), phase 1): `list`/`add`/`use`/`remove`/`rename` on a structured `~/.config/hive/profiles.yml` (mode 0600), with automatic one-time migration from the positional `contributor.env` lists and a regenerated `contributor.env` projection that always keeps the hub/token/id lists aligned. `use` writes the active hive first, where the relay starts soliciting on its next start; a live switch without a restart is phase 2.
