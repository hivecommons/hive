- Removed the dead `dashboard.snapshot_dir` config knob for good ([#6192](https://github.com/hivecommons/hive/issues/6192)). The docs templates lost the row in [#6193](https://github.com/hivecommons/hive/pull/6193); this finishes the job by dropping the last remaining template line from `bin/hive-setup.sh` and deleting the never-read `DashboardConfig.SnapshotDir` Go field. Nous baseline snapshots keep using their hardcoded `/data/nous/snapshots` path and the public `/snapshot` page is still driven by `hub.auto_snapshot`, so nothing changes at runtime. Existing `hive.yaml` files that still set the key keep loading unchanged - the config loader is not strict and silently ignores unknown keys.
