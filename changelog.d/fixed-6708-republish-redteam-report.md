- The `ioscan` red-team evaluation is republished after the Unicode fixes, unbreaking `v4` ([#6708](https://github.com/hivecommons/hive/issues/6708)). Closing the U+FF29 fullwidth and U+00AD soft-hyphen steganography gaps moved U05 and U08 from `missed` to `blocked`, which is exactly what `TestRedTeamCorpusOutcomesAreStable` is built to catch: the ratchet fails on an *improvement* too, because an improvement makes the published miss rate wrong. The fix landed without regenerating the page, so the default branch went red and stayed red for four consecutive commits. The measured numbers are now correct everywhere they appear — the Unicode family goes from 6/8 to **8/8 blocked**, and overall **42% of attack payloads are withheld from the agent, 58% reach it verbatim** (was 37%/63%), updated in `ioscan-red-team.md`, the security self-assessment, and the docs index. The published-outcome table in `redteam_test.go` is updated in the same commit, which is the step that was missed. The rewritten "Rule gaps" section states plainly that two characters were added to two tables and that 8/8 against this corpus is not Unicode evasion being solved — the next unmapped confusable does the same thing again.
