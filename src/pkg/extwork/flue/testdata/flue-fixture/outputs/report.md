# Fixture report

- `triage` returns `needs-repro` for bodies without the word "reproduction".
- No preview package was published; no comment was posted; no PR was opened.
- Candidate patch attached as `patch.diff`; it is a claim for Hive to verify.
