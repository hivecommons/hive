apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vllm-deployment.yaml
  # Durable RBAC + InferencePool for the live hive-inference EPP (issue #2341).
  - hive-epp-rbac.yaml
